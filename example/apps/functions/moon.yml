# https://moonrepo.dev/docs/config/project
$schema: "https://moonrepo.dev/schemas/project.json"

tasks:
  build:
    command: 'firestack build src/controllers/api/test_api.ts dist/api/src/index.js --external firebase-admin,firebase-functions'
    inputs:
      - "src/**/*"
      - "firestack.json"
    options:
      cache: false

  deploy:
    command: 'bun run deploy'
    inputs:
      - "src/**/*"
      - "firestack.json"
    options:
      cache: false

  delete:
    command: 'bun run delete'
    inputs:
      - "src/**/*"
      - "firestack.json"
    options:
      cache: false

  emulate:
    command: 'bun run emulate'
    inputs:
      - "src/**/*"
      - "firestack.json"
    options:
      cache: false
      persistent: true

  rules:
    command: 'bun run rules'
    inputs:
      - "src/**/*"
      - "firestack.json"
    options:
      cache: false

  scripts:
    script: 'bun run scripts'
    inputs:
      - "src/**/*"
      - "scripts/**/*"
      - "firestack.json"
    options:
      cache: false
      interactive: true
  logs:
    command: 'bun run logs'
    inputs:
      - "src/**/*"
      - "firestack.json"
    options:
      cache: false
  check:
    command: 'bun run check'
    inputs:
      - "src/**/*"
      - "scripts/**/*"
      - "firestack.json"
    options:
      cache: false
  fix:
    command: 'bun run fix'
    inputs:
      - "src/**/*"
      - "scripts/**/*"
      - "firestack.json"
    options:
      cache: false
