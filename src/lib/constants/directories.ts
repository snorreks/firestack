/**
 * The list of directories that are considered for deployment.
 */
export const deployDirectories = [
  'api',
  'https',
  'callable',
  'database',
  'auth',
  'firestore',
  'scheduler',
  'storage',
] as const;
